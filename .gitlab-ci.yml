stages:
  - deploy

production:
  image:
    name: docker:27.4.1
  stage: deploy
  only:
    - production
  tags:
    - production
  before_script:
    - docker version
  script:
    - docker image prune -f
    - docker compose build --no-cache
    - docker compose up -d
